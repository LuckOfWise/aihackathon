<div class="cta-banner">
  <div class="cta-banner__text">
    <h3 class="cta-banner__title"><%= title %></h3>
    <% if lede.present? %>
      <p class="cta-banner__lede"><%= lede %></p>
    <% end %>
  </div>
  <% if action? %>
    <%= action %>
  <% end %>
</div>
